Recommended way to build and run:

- Install VSCode and Playdate Debug extension
- Click the "play" button to run

Or, using `Makefile` to build and run:

- `make build`
- `make run` for build and run